name: Daily Commit

on:
  schedule:
    - cron: '30 0 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Update log file
        run: |
          echo "Contribution on $(date -u)" >> contributions.log

      - name: Commit and push
        run: |
          git config user.name "prince1205-code"
          git config user.email "princekumar16u@gmail.com"
          git add contributions.log
          git commit -m "Daily auto-contribution: $(date -u +%F)"
          git push
